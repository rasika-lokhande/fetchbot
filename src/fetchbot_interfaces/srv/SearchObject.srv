# Request
string target_object    # Object to search for (e.g., "red cup")
---
# Response
bool success           # True if object found
float32 confidence     # Detection confidence (0.0 to 1.0)
string message         # Status message